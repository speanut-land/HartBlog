{
  "name": "web-fundamental",
  "version": "1.0.0",
  "description": "this is pure ts code finish a small frame, Write casually",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "npm run clean-build-steps && rm -rf dist"
  },
  "author": "speanut",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "babel-preset-env": "^1.7.0",
    "typescript": "^3.8.3",
    "webpack": "^4.43.0"
  }
}
